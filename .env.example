# eCDF prototype — placeholders only. Never commit a real secret.
NODE_ENV=development
LOG_LEVEL=info

# Safety boundary: testnet only.
STELLAR_NETWORK=testnet
STELLAR_RPC_URL=https://REPLACE_WITH_TESTNET_RPC_ENDPOINT

# Disposable Testnet key only at runtime. This placeholder is intentionally invalid.
STELLAR_SECRET_KEY=REPLACE_WITH_DISPOSABLE_TESTNET_SECRET_AT_RUNTIME

# Final safe limit will be defined before live integration in INC-03.
MAX_TEST_AMOUNT_XLM=REPLACE_BEFORE_LIVE_TEST

EVIDENCE_DIR=./artifacts/runs
